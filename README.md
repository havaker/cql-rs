CQL Rust async driver prototype

```bash
# building
cargo build

# testing
cargo test

# running example (it will try to connect to scylla on 127.0.0.1:9042 and send two inserts)
cargo run --example simple
```
